# `git archive` (used by release-reusable.yml to produce the mumei
# plugin tarball) excludes paths marked here. The shared JSON Schemas
# under schemas/ are development-time contracts consumed by the bash
# hooks at authoring time, not read at runtime, so they do not belong
# in the plugin tarball.
#
# Adding entries here is a release-shape change — verify the next
# tarball still installs cleanly into Claude Code.

schemas/ export-ignore

# Maintainer-side Claude Code assets. These are tracked team assets for
# developing mumei (project instructions, dev rules, the release and
# changelog-watch skills), not part of the plugin surface: Claude Code
# loads a plugin through .claude-plugin/plugin.json, which points only at
# agents/, skills/ and hooks/. Shipping them would put maintainer-only
# skills in a plugin user's tarball.
# Both patterns are root-anchored: gitattributes uses gitignore matching,
# so a bare `CLAUDE.md` would also strip examples/sample-project/CLAUDE.md
# from the tarball and ship an incomplete sample project.
/.claude/ export-ignore
/CLAUDE.md export-ignore
